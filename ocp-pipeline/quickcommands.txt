Run in order (from the ./ocp-pipeline root):

Task 1              |   oc apply -f fileReceivedTask.yaml 
Task 2              |   oc apply -f fileReceivedFinisherTask.yaml
Pipeline            |   oc apply -f pipeline-received.yaml 
Trigger Template    |   oc apply -f ./triggers/triggertemplate1.yaml
Trigger Binding     |   oc apply -f ./triggers/triggerbinding1.yaml  
Event listener      |   oc apply -f ./triggers/eventtrigger1.yaml   
Route               |   oc apply -f ./routes/eventlistenerRoute.yaml 


TESTING:
Open up Postman or any API mgmt tool
or;
git commit -a -m "build commit" --allow-empty           #this triggers the webhook



URL: http://route-aiodyssey-ai-odyssey.apps.cluster-r6s5g.r6s5g.sandbox2145.opentlc.com
Content-type : application/json
Params: filereceived (must be in the BODY of a JSON object)

oc get pr                               # gets the current pipelines in use
tkn pipeline list                       # gets the current pipelines in use
oc get service                          # gets the current services running (important for event listener) #default created is el-eventlistener1
oc logs -f svc/el-eventlistener1        # watchers for incoming traffic

tkn pipeline describe pipeline-received # describes the pipeline